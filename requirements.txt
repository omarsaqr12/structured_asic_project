# Python dependencies for the structured-ASIC physical-design flow.
# Install with:  pip install -r requirements.txt
#
# Note: routing and static timing analysis (route.tcl / sta.tcl) additionally
# require OpenROAD (https://github.com/The-OpenROAD-Project/OpenROAD), which is
# an external binary and not installable via pip.

numpy>=1.21
scipy>=1.7
matplotlib>=3.4
PyYAML>=5.4
tqdm>=4.60

# Testing
pytest>=6.0
